/**
 * Foreground for labels on amber `secondary.main` nav drawers.
 * Near-black keeps WCAG contrast on #fbc02d / #ffd54f; avoids MUI `text.primary` fighting the yellow bar.
 */
export const DRAWER_FOREGROUND = "#141414";
