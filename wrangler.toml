# Cloudflare Pages — React (Create React App) static deploy
# Git-connected Pages reads this file when pages_build_output_dir is set.
# Dashboard: set build command to `npm run build` if not inherited automatically.

name = "distribution-prime"
compatibility_date = "2025-04-01"
pages_build_output_dir = "./build"
